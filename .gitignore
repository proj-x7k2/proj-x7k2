# 절대 GitHub에 올라가면 안 되는 파일
.env
claude_token.txt

# 원자료 층 (논문 전문 PDF·원문은 공개 저장소에 올리지 않음, 로컬에만 보관)
raw/

# 실행할 때마다 바뀌는 파일
logs/
data/seen_ids.json
data/raw_papers.json
data/pdf_done.json
data/last_lint.txt

# macOS / Obsidian 개인 화면 설정 (열 때마다 바뀌어서 불필요한 커밋이 생김)
.DS_Store
obsidian_notes/.obsidian/workspace*.json

# Python 캐시
__pycache__/
*.pyc
