# 수의영상의학 논문 LLM 위키 — 관리 스키마

이 프로젝트는 PubMed에서 자동 수집된 수의영상의학(MRI/CT/초음파) 논문을 기반으로
**누적되는 위키**를 유지합니다. 단순히 논문마다 요약 카드를 쌓는 게 아니라,
새 논문이 들어올 때마다 관련 주제 페이지를 찾아 통합하고, 전체 개관을 계속 갱신합니다.

## 세 개의 레이어

1. **원본 데이터** (`data/raw_papers.json`) — 이번 실행에서 새로 수집된 논문 원문
   (제목/초록/저자/저널/DOI). 읽기만 하고 절대 수정하지 않습니다. 진실의 원천입니다.
2. **위키** (`obsidian_notes/` 폴더) — 아래 세 종류의 파일로 구성되며, 당신(Claude)이
   전적으로 관리합니다.
3. **이 문서(CLAUDE.md)** — 위키 관리 방식 자체의 정의입니다. 작업하다가 구조를
   개선할 아이디어가 떠오르면, 사용자에게 먼저 확인 없이 이 문서 자체도 함께
   업데이트해도 좋습니다 (단, 기존 파일 형식을 바꾸는 큰 변경은 사용자에게 먼저 물어보세요).

## 위키 구조

```
obsidian_notes/
  papers/{year}-{slug}.md      논문 한 편당 한 페이지 (원자료 + 요약)
  topics/{topic-slug}.md       주제별 종합 페이지 (여러 논문이 계속 기여)
  overview.md                  위키 전체의 현재 상태를 보여주는 최상위 개관
```

## 새 논문을 처리하는 워크플로우 (매주 실행됨)

1. `data/raw_papers.json`을 읽습니다. 빈 배열이면 "새 논문 없음"만 출력하고 종료합니다.
2. **논문마다 순서대로**:
   a. `obsidian_notes/papers/{year}-{slug}.md` 에 논문 페이지를 만듭니다
      (형식은 아래 "논문 페이지 형식" 참고). 파일명 slug는 제목을 소문자·하이픈으로 변환.
   b. 이 논문이 어떤 기존 주제와 관련 있는지 판단합니다
      (예: MRI 프로토콜, 종양학 영상, 심장 초음파, 신경계 영상 등 — 주제는 논문
      내용에 맞게 자유롭게 정하되, 너무 잘게 쪼개지 말고 재사용 가능한 수준으로).
   c. **관련 topic 페이지가 이미 있으면 반드시 먼저 읽고**, 그 내용을 유지한 채
      새 발견을 통합해서 업데이트하세요. 절대 기존 내용을 통째로 덮어쓰지 마세요.
      없으면 새로 만드세요.
   d. 만약 새 논문이 기존 topic 페이지의 결론과 **다르거나 상충되는 근거**를
      제시한다면, 조용히 덮어쓰지 말고 topic 페이지 안에 "## 상충되는 근거" 섹션을
      만들어 명시적으로 남기세요. 어느 쪽이 맞다고 임의로 판단해서 지우지 마세요.
   e. 논문 페이지와 관련 topic 페이지 사이에 Obsidian 링크(`[[topic-slug]]`,
      `[[papers/{연도}-{slug}]]`)를 서로 걸어주세요.
3. 모든 논문 처리가 끝나면 `obsidian_notes/overview.md`를 읽고 갱신합니다:
   - "최근 업데이트" 섹션에 오늘 날짜, 이번에 처리한 논문 수, 새로 생기거나
     갱신된 주제를 한두 줄로 추가 (기존 로그는 남겨두고 위에 새로 추가).
   - "주요 주제" 목록에 새 topic이 생겼으면 추가.
4. `data/papers.json`을 읽어서, 이번에 처리한 논문들을 배열 맨 앞에 추가합니다
   (필드: pmid, title, journal, year, authors, url, doi, summary_en, summary_ko,
   clinical_takeaway_ko, tags, added_on — added_on은 오늘 날짜).
5. 마지막에 몇 편을 처리했고 어떤 topic이 갱신/신설됐는지 한두 줄로 요약해서 출력하세요.

## 논문 페이지 형식 (obsidian_notes/papers/*.md)

```markdown
# {제목}

- **저널**: {저널명} ({연도})
- **저자**: {저자 목록}
- **PubMed**: {url}
- **DOI**: {doi}

관련 주제: [[topics/{topic-slug}]]

#{태그1} #{태그2} #{태그3}

## 국문 요약
{3-4문장}

## English summary
{3-4 sentences}

## 임상 시사점
{1-2문장, 국문}

## 원문 초록
{abstract 원문}
```

## 주제 페이지 형식 (obsidian_notes/topics/*.md)

```markdown
# {주제명}

## 현재까지의 종합
{이 주제에 대해 지금까지 쌓인 논문들을 바탕으로 한 종합적 결론.
새 논문이 추가될 때마다 다시 읽고 갱신 — 처음부터 다시 쓰지 말고 기존 문장에
새 근거를 반영해서 자연스럽게 통합}

## 관련 논문
- [[papers/{연도}-{slug}]] — 한 줄 핵심 내용
- [[papers/{연도}-{slug}]] — 한 줄 핵심 내용

## 상충되는 근거
{있을 때만 작성. 어떤 논문이 어떤 점에서 다른 결론을 내는지 구체적으로}
```

## overview.md 형식

```markdown
# 수의영상의학 논문 위키 개관

## 주요 주제
- [[topics/{topic-slug}]] — 한 줄 설명
- [[topics/{topic-slug}]] — 한 줄 설명

## 최근 업데이트
- {날짜}: 논문 {N}편 처리. {새로 생기거나 갱신된 주제 요약}
- {이전 날짜}: ...
```

## 스타일 원칙

- 모든 서술은 국문으로 작성하되, **의학/해부학/영상의학 전문 용어(질환명,
  해부학적 구조, 영상 기법, 약물명 등)는 국문으로 번역하지 말고 영어 원어를
  그대로 사용**하세요. 조사와 문장 흐름만 자연스럽게 국문으로 연결하면 됩니다.
  - 예 (권장): "이 종양은 T2-weighted 영상에서 hyperintense 신호를 보였다."
  - 예 (지양): "이 종양은 T2 강조 영상에서 고신호강도를 보였다."
  - 전문 용어가 아닌 일반 서술(예: "~로 나타났다", "~와 관련이 있다")은
    평소대로 자연스러운 국문 문장으로 씁니다.
- 임상 수의사가 실무에 바로 적용할 수 있는 관점을 항상 하나 이상 포함.
- topic 페이지의 "종합" 문단은 매번 새로 쓰지 말고, 기존 문장 구조를 최대한
  살리면서 새 근거만 자연스럽게 덧붙이거나 다듬는 방식으로 갱신 (히스토리를
  존중하는 편집).
